<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.finaltest.startfit">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name = "android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/sficon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sficon"
        android:supportsRtl="true"
        android:theme="@style/Theme.StartFit">

        <activity android:name=".LodingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.RegisterActivity"/>
        <activity android:name=".login.LoginActivity"/>
        <activity android:name=".login.passwordresetActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>
        <activity android:name=".calender.timer2Activity" /> <!-- 운동 관련 activity 파일 -->
        <activity android:name=".video.VideoView" />
        <activity android:name=".video.exercise_video.exercise_upper_body" />
        <activity android:name=".video.exercise_video.exercise_lower_body" />
        <activity android:name=".video.exercise_video.exercise_full_body" />
        <activity android:name=".video.exercise_video.exercise_running" />
        <activity android:name=".MemberInitActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".WritePostActivity"/>
        <activity android:name=".PostActivity"/>
        <activity android:name=".camera.CameraActivity" >

        </activity>

    </application>

</manifest>